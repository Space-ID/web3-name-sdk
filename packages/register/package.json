{
  "name": "@web3-name-sdk/register",
  "version": "0.0.2-beta.3",
  "description": "",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm --clean",
    "lint": "prettier --write .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "pnpm run build"
  },
  "files": [
    "dist/**"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsup": "^7.1.0"
  },
  "peerDependencies": {
    "@siddomains/sid-contracts": "^0.1.31",
    "@siddomains/sidjs": "^0.1.28",
    "@web3-name-sdk/core": "^0.1.16",
    "ethers": "^5.7.2"
  },
  "dependencies": {
    "@siddomains/sid-contracts": "^0.1.31",
    "@siddomains/sidjs": "^0.1.28",
    "@web3-name-sdk/core": "^0.1.16",
    "ethers": "^5.7.2",
    "viem": "^2.20.0"
  }
}
